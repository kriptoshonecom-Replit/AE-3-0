/**
 * PIT Services — Hourly Rate
 * This is the single place to change the fixed rate used to calculate
 * all PIT line item prices: price = duration (hrs) × PIT_HOURLY_RATE
 */
export const PIT_HOURLY_RATE = 120.00;
